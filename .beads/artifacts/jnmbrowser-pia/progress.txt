# Progress Log

Bead: jnmbrowser-pia
Started: 2026-07-28

## Codebase Patterns

- BrowserRunner owns browser launch, process lifecycle, pre-launch rollback, fingerprint regeneration, and ephemeral directory handling.
- ephemeral_dirs.rs owns transactional runtime directory replacement, deletion retries, startup recovery, and effective-path resolution.
- Two-factor Backfill provides the strongest existing cleanup guard, ownership check, rollback, and stale-recovery pattern.
- Auto Registration already tracks worker ownership and has backend profile_id validation, but normal UI wiring and immutable runtime adoption are incomplete.
- All user-visible frontend strings must be translated in seven locale files; Tauri listeners must be installed before starting work.

## Specification Notes

- Selected architecture: Option C (Recommended), using immutable source profiles, runtime launch policy, runtime data/fingerprint overrides, and a full lifecycle lease.
- `/create` intentionally writes specification artifacts only. Application implementation belongs to `/ship jnmbrowser-pia` after planning and file reservation.
- Existing dirty worktree changes on feat/auto-reg-2fa-backfill are pre-existing and must remain untouched.

---

<!-- Task logs below - APPEND ONLY -->

## 2026-07-28 — /plan jnmbrowser-pia

- Wrote `.beads/artifacts/jnmbrowser-pia/plan.md` (Option C, Level 2 discovery).
- Locked: fail-fast lease, profile_runtime module, lease-scoped ephemeral keys, no source metadata FP writes, UI Select by profile id.
- Waves 1–9 map to PRD tasks backend-1 … verification-1.
- No application code implemented in /plan.


## 2026-07-28 — /ship jnmbrowser-pia Wave 1

- Implemented `src-tauri/src/profile_runtime/` (policy, lease, guard, mod) + `mod profile_runtime` in lib.rs.
- Tests: `cargo test --lib profile_runtime::` → 13 passed.
- Commit: `7a4ea54 feat(profile-runtime): add launch policy types and lifecycle lease registry`.
- Pre-commit gate required small unrelated test/clippy alignment (UA 148, firefox BrowserType, password radio selector, account-check-live match_result_ok).
- backend-1 remains `passes: false` until Wave 2 (ephemeral keys + BrowserRunner policy path) completes PRD steps.
- Next: Wave 2 Task 2.

## 2026-07-29 — /ship jnmbrowser-pia Wave 2

- Implemented lease-scoped ephemeral directory APIs with backward-compatible profile-id wrappers.
- Added BrowserRunner policy launch overlays for runtime-only data, fingerprint, proxy/VPN, metadata persistence, and PID tracking.
- Added manager-aware, ownership-safe, retryable runtime cleanup for browser, proxy, owned VPN worker, and ephemeral data; lifecycle lease release remains workflow-owned.
- Verification: `ephemeral_dirs::` 15 passed; `profile_runtime::` 14 passed; `browser_runner::` 20 passed; full `cargo test --lib` 912 passed; formatting, focused clippy, and `git diff --check` passed.
- Commit: `22a2b34 feat(profile-runtime): add policy-aware browser launch overlays`.
- PRD `backend-1` complete; next: Wave 3 Task 3 (Account Checker vertical slice).

## 2026-07-29 — /ship jnmbrowser-pia Wave 3

- Migrated Account Checker to `AccountCheckConfig` with selected profile, data mode, fingerprint mode, and fixed/rotating VPN policy.
- Added synchronous selected-profile validation and fail-fast lease acquisition, read-only metadata loading, generated-worker ownership controls, cancellation truthfulness, and guarded cleanup diagnostics.
- Applied effective VPN to the actual BrowserRunner runtime policy; selected profile metadata and persistent data remain untouched during validation and launches.
- Added failed-launch rollback synchronization before runtime cleanup/lease release.
- Verification: Account Checker 35 passed; BrowserRunner 20 passed; full `cargo test --lib` 925 passed; all-targets/all-features clippy, formatting, unused-command check, and diff check passed.
- Commit: `ed90af6 feat(account-checker): reuse selected profiles with runtime policy`.
- PRD `backend-2` complete; next: Wave 4 Task 4 (Auto Registration migration).

## 2026-07-29 — /ship jnmbrowser-pia Wave 4

- Added Auto Registration `dataMode` and `fingerprintMode` request defaults while preserving legacy payload compatibility and selected-profile concurrency rejection.
- Added synchronous read-only selected-profile validation and fail-fast lease acquisition before task publication; missing, running, cross-OS, browser-mismatched, busy, and unsupported ephemeral Camoufox/Firefox selections fail closed.
- Removed selected-profile fingerprint/proxy/VPN metadata updates and routed selected launches, relaunches, attach-failure cleanup, and per-account cleanup through BrowserRunner runtime policy APIs with task-local network overrides.
- Added guarded cleanup that releases the lease only after runtime resources are reconciled, defers selected batch terminal events until cleanup, and keeps failed cleanup leases unavailable for overlap.
- Preserved generated-worker creation/ordinary launch/deletion behavior; deletion now requires matching task ownership. Parallel batch requests now prepare every config and lease before spawning the first task.
- Verification: Registration 131 passed; `profile_runtime::` 14 passed; `browser_runner::` 20 passed; full `cargo test --lib` 942 passed; focused all-features lib clippy, Rust formatting, unused-command check, and diff check passed. Existing-account 2FA dispatch tests remain green inside the Registration suite.
- PRD `backend-3` complete; next: Wave 5 Task 5 (Auto Login migration).

## 2026-07-29 — /ship jnmbrowser-pia Wave 5

- Added Auto Login `profileId`, `dataMode`, and `fingerprintMode` request defaults while preserving current frontend payload compatibility.
- Moved worker resolution to synchronous preparation: explicit profiles are validated read-only, while the no-profile path reuses only the exact `auto-login-worker-{browser}` profile or creates that exact stable worker once under a preparation lease.
- Added fail-fast profile-ID leases before task publication; same-profile starts are rejected immediately and prepared-task drop releases the lease before execution begins.
- Removed broad `auto-login-worker-*` matching and all profile fingerprint/proxy/VPN mutation calls; selected and stable default workers launch through task-local BrowserRunner policy overlays.
- Added policy-aware launch, failed-launch rollback synchronization, Camoufox/CDP attach-failure cleanup, per-login runtime cleanup, final guarded reconciliation, lease-after-cleanup release, and deferred batch terminal events.
- Auto Login retains both selected profiles and its exact stable default worker; no profile deletion path remains in the engine.
- Verification: Auto Login 68 passed; full `cargo test --lib` 955 passed; focused all-features lib clippy, Rust formatting, unused-command check, and diff check passed. Unused-command bin targets emitted only the existing Windows `LNK4075` linker warning.
- PRD `backend-4` complete; next: Wave 6 Task 6 (2FA Backfill migration).

## 2026-07-29 — /ship jnmbrowser-pia Wave 6

- Added backward-compatible 2FA Backfill `profileId`, `dataMode`, and `fingerprintMode` fields to start and preview requests while preserving generated-worker defaults.
- Added synchronous read-only selected-profile validation and fail-fast lease acquisition before task publication; missing, invalid, running, cross-OS, browser-mismatched, busy, and unsupported ephemeral Camoufox selections fail closed.
- Added one task-scoped selected runtime context reused across the existing serial account loop; data, fingerprint, proxy, VPN, and direct-network settings are applied only through BrowserRunner runtime policy overlays.
- Selected launches, attach/prepare rollback, per-account close, cancellation, and task-end cleanup use policy-aware runtime cleanup without deleting or mutating the source profile.
- Cleanup validates profile, policy, lease source, and active lease holder; cleanup failures retain the lease and keep the guard armed for retry, while successful final cleanup releases it.
- Preserved generated-worker creation, exact operation-name/ephemeral ownership checks, deletion, stale recovery, canary/bulk gates, journals, CAS ownership, cancellation, and secret-free progress/recovery payloads unchanged.
- Verification: 2FA Backfill 100 passed; full `cargo test --lib` 965 passed; all-targets/all-features clippy, Rust formatting, unused-command check, and diff check passed.
- PRD `backend-5` complete; next: Wave 7 Task 7 (frontend policy controls and Tauri wiring).

## 2026-07-29 — /ship jnmbrowser-pia Wave 7

- Added one shared automation profile-policy contract and field group across Account Checker, Auto Registration, Auto Login, and 2FA Backfill. Select values are profile UUIDs; generated workers remain the default.
- Added the read-only `list_automation_leased_profile_ids` command, which returns only sorted source profile IDs. Running and leased profiles are disabled, and stale or newly unavailable selections are cleared without exposing lease/task/runtime IDs.
- Wired `profileId`/`sourceProfileId`, `dataMode`, `fingerprintMode`, and compatible network fields through all four start payloads; Account Checker supports an optional fixed VPN while preserving default rotation.
- Registration clamps and disables concurrency at one for a selected source profile. Camoufox selection forces persistent data and disables ephemeral mode. Auto Login retries preserve the selected runtime policy, and 2FA preview/start requests use identical policy fields.
- Preserved listener-before-invoke ordering for Checker, Registration, Login, and 2FA Backfill; new helper and ordering tests cover defaults, payload mapping, UUID normalization, concurrency, and StrictMode listener setup.
- Production-export browser verification used a pre-document mocked Tauri bridge with stopped/running/leased Chromium profiles and stopped Camoufox. All four dialogs rendered without page errors; running/leased options were disabled; keyboard `Enter` opened selectors; UUID policy payloads reached Tauri; Camoufox forced persistent mode; Registration concurrency stayed one; and 2FA preview/start policy payloads matched.
- Responsive verification at 390×844 found no horizontal clipping in Checker, Registration, Login, or 2FA Backfill. Registration's below-fold concurrency control remained reachable through its internal vertical scroll; the 2FA footer and selected-account summary remained visible.
- Verification: `pnpm test:frontend` 28 passed; `pnpm lint:js` passed 157 root and 12 donut-sync files with TypeScript clean; `pnpm check-unused-commands` 1 passed; `cargo test --manifest-path src-tauri/Cargo.toml --lib profile_runtime::` 15 passed; scoped diff checks passed.
- PRD `frontend-1` complete; next: Wave 8 Task 8 (seven-locale policy terminology and lifecycle documentation).

## 2026-07-29 — /ship jnmbrowser-pia Wave 8

- Added one shared `automationProfile` namespace with 19 identical, non-empty keys in `en`, `es`, `fr`, `ja`, `pt`, `ru`, and `zh`. It names generated workers, running versus automation-leased profiles, persistent/ephemeral data, stable/random fingerprints, the Camoufox/Firefox limitation, selected-profile concurrency, and safe error categories.
- Added a closed frontend error classifier for known busy, selected-profile validation, and cleanup markers. Dialogs render only translated keys; unknown backend text is never displayed. Account Checker now preserves start failures for classification, while Registration, Auto Login, and 2FA Backfill no longer borrow the generic 2FA backend error.
- Updated the shared field group so unchecked modes are explicitly labeled as persistent source data and stable stored fingerprint. Running and leased selector options are distinguished, and Registration explains why a selected profile forces concurrency to one.
- Replaced the registration-only lifecycle note with a shared contract for Account Checker, Auto Registration, Auto Login, and 2FA Backfill: generated versus selected ownership, UUID selection, runtime-only network overlays, immutable source metadata, fail-fast process-local leases, cleanup-failure retention, verified generated-worker deletion, and a Chromium/Camoufox/Firefox capability matrix.
- Documented the process-local registry limitation: restart clears the in-memory lease but does not prove an orphaned browser exited, so operators must independently confirm no process remains before reuse.
- Verification: `pnpm test:frontend` 29 passed; `pnpm lint:js` passed 157 root and 12 donut-sync files with TypeScript clean; locale checker passed 7 locales with 1800 keys each; dedicated namespace audit passed 19 identical non-empty keys; scoped Biome, typos, and diff checks passed.
- PRD `docs-1` complete; next: Wave 9 Task 9 (full repository quality gate and completion audit).
